import Cookie from '../Cookie'

describe('Cookie Model', () => {
  let CookieModel;
  beforeEach(() => {
    CookieModel = new Cookie()
  })
  it('has some cookies', () => {
    // throw new Error('no cookies here')
  })
  it('has some more cookies', () => {
    // throw new Error('no cookies here')
  })
})
